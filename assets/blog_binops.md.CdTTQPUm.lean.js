import{_ as a,c as t,a2 as n,o as s}from"./chunks/framework.CoXjB5sU.js";const m=JSON.parse('{"title":"Binary operators in Aya","description":"","frontmatter":{},"headers":[],"relativePath":"blog/binops.md","filePath":"blog/binops.md","lastUpdated":1717413547000}'),i={name:"blog/binops.md"};function o(r,e,p,c,l,d){return s(),t("div",null,e[0]||(e[0]=[n(`<h1 id="binary-operators-in-aya" tabindex="-1">Binary operators in Aya <a class="header-anchor" href="#binary-operators-in-aya" aria-label="Permalink to &quot;Binary operators in Aya&quot;">​</a></h1><p>We have designed a binary operator system in Aya which happens to be (we didn&#39;t copy!) very similar to <a href="https://plt.cs.northwestern.edu/pkg-build/doc/enforest/Operator_Precedence_and_Associativity.html" target="_blank" rel="noreferrer">Rhombus</a> (a.k.a. Racket 2) and <a href="https://docs.swift.org/swift-book/ReferenceManual/Declarations.html#ID550" target="_blank" rel="noreferrer">Swift 5.7</a>.</p><p>TL;DR: it supports making <em>any</em> identifier a custom operator with precedences specified by a partial ordering. Left and right associativities are supported.</p><p>The precedence and associativity information is bound to a name, not a definition. This means we can import a name from another module with changes to its name, associativity, and precedence. Importing with renaming is an established feature, but changing associativity and precedence is not that popular (though implemented in Agda already).</p><p>Here are some code examples (implementations are omitted for simplicity):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>-- Left-associative</span></span>
<span class="line"><span>def infixl + (x y : Nat) : Nat =&gt; {??}</span></span>
<span class="line"><span>-- Left-associative, bind tighter than +</span></span>
<span class="line"><span>def infixl * (x y : Nat) : Nat =&gt; {??} tighter +</span></span>
<span class="line"><span>-- Prefix operator</span></span>
<span class="line"><span>def fixl ! (x : Nat) : Nat =&gt; {??}</span></span>
<span class="line"><span>-- Postfix operator</span></span>
<span class="line"><span>def fixr ? (x : Nat) : Nat =&gt; {??}</span></span></code></pre></div><p>The <code>tighter</code> keyword works like this: when there are expressions like <code>a * b + c</code> which may either mean <code>(a * b) + c</code> or <code>a * (b + c)</code>, we will put the tighter operator in the parenthesis. In case we found the two operators share the same priority, Aya will report an error.</p><p>With imports, it looks like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>open import Primitives using (</span></span>
<span class="line"><span>  invol       as fixl  ~  tighter =, \\/, /\\,</span></span>
<span class="line"><span>  intervalMin as infix /\\ tighter \\/,</span></span>
<span class="line"><span>  intervalMax as infix \\/,</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>Specifying operator precedences with a partial ordering is way better than with a number. In Haskell, if we already have <code>infix 3 +</code> and <code>infix 4 *</code> and we hope to add a new operator which has higher precedence than <code>+</code> but lower than <code>*</code>, it&#39;s going to be impossible. Agda introduced <a href="https://github.com/agda/agda/issues/3991" target="_blank" rel="noreferrer">float-point precedence</a> levels to address the issue, but I think it does not solve the essential problem: that I have to lookup the numbers (of existing operator precedences) every time I write a new operator.</p><p>In the future, we plan to support mixfix operators as in Agda (the current framework can support mixfix easily, but abusing mixfix notations can harm readability).</p>`,11)]))}const f=a(i,[["render",o]]);export{m as __pageData,f as default};
